import React from 'react';
import { ListGroup } from 'react-bootstrap';
import JobListItem from './JobListItem'

export default (props) => {

  props.newSearchResults.map((searchResult, index) => (

  )

  return (
    <div className="job-list-box">
      <ListGroup>
        {}
      </ListGroup>
    </div>
  )
}
