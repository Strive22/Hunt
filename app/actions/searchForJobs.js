import axios from 'axios'
import { GET_GITHUB } from './actionTypes';

function searchForJobs(details){

  // let response = axios.get(`/search/gh/${details.keyword}`)
  // return { type: GET_GITHUB, payload: response}
}
